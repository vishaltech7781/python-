name = " yash is good boy he is very kind  boy "

print (name.replace("  "," "))
print (name) #string are immutable though u apply any function abiut they can't be
        #changed in the code the changes only visible of function in the output only 